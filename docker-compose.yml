version: '3'

services:
    brevets:
        build:
          context: ./brevets
          dockerfile: Dockerfile
        ports: 
          - "5001:5000"
        depends_on:
          - mongo
        volumes:
          - ./brevets/:/app
    mongo:
      image: 427dd68dd642
